NumeroX, NumeroY = map(int, input().split()) #el map como que une las dos cuestiones y con el uso de .split los separa para luego
                                             #poder comparlos y tal

if NumeroX > NumeroY:
    print(NumeroX)
else:
    print(NumeroY)